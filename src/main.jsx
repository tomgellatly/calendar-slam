import React from 'react'
import ReactDOM from 'react-dom/client'
import CalendarSlam from '../calendar-slam.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CalendarSlam />
  </React.StrictMode>,
)
